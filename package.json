{
  "devDependencies": {
    "@cypress/grep": "^3.1.3",
    "@percy/cli": "^1.16.0",
    "@percy/cypress": "^3.1.2",
    "cypress": "^12.1.0",
    "npx": "^10.2.2"
  },
  "scripts": {
    "cypress:percy": "percy exec -- cypress run --spec 'cypress/e2e/meetOurTeam.cy.js'"
  }
}
